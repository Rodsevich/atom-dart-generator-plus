'use babel';

import AtomDartGeneratorPlusView from '../lib/atom-dart-generator-plus-view';

describe('AtomDartGeneratorPlusView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
